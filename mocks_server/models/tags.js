const DEFAULT_MODEL = {
  tag: '',
}

module.exports = class Tags {
  constructor() {
    // this.users = [...usersData.results]
  }

  addTag = ({ ...DEFAULT_MODEL }) => {
    const tags = []
    return tags
  }

  getTags = () => {
    const tags = []
    return tags
  }
}
